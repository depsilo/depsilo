# Dark Mode Integration Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the `handoff_dark_mode` design tokens and ThemeToggle into the Portal (QuickStart + Monitor pages), including page-level aurora effects.

**Architecture:** Dark tokens drop into `index.css` under `html.dark` — all components already use `var(--)` variables so they adapt automatically. ThemeToggle stub is replaced with the functional three-state control from the handoff. An inline script in `index.html` prevents flash-of-wrong-theme on load.

**Tech Stack:** React 18 + TypeScript, Tailwind v4, CSS custom properties, localStorage

---

## File Map

| File | Change |
|---|---|
| `web/src/index.css` | Add dark token block + dark overrides for `.page-wash` and `.aurora-glow` |
| `web/src/components/ThemeToggle.tsx` | Replace disabled stub with functional Light·Dark·Auto segmented control |
| `web/index.html` | Add anti-FOUC inline script in `<head>` |
| `web/src/portal/PortalApp.tsx` | Add `<div className="page-wash" />` as first child of root div |

---

### Task 1: Add dark tokens to index.css

**Files:**
- Modify: `web/src/index.css` (after the closing `}` of the `:root` block, around line 143)

- [ ] **Step 1: Append dark token block and effect overrides**

Insert after the `:root { ... }` block (after line 143):

```css
/* ── Dark mode tokens ── */
html.dark,
[data-theme="dark"] {
  --bg-page:   oklch(0.18 0.012 250);
  --bg-panel:  oklch(0.20 0.012 250);
  --bg-card:   oklch(0.22 0.013 250);
  --bg-soft:   oklch(0.25 0.014 250);
  --bg-hover:  oklch(0.32 0.016 250 / 0.45);

  --border:        oklch(0.92 0.01 250 / 0.10);
  --border-soft:   oklch(0.92 0.01 250 / 0.05);
  --border-strong: oklch(0.92 0.01 250 / 0.22);

  --text:        oklch(0.97 0.005 250);
  --text-muted:  oklch(0.74 0.012 250);
  --text-soft:   oklch(0.62 0.012 250);
  --text-subtle: oklch(0.52 0.012 250);

  --brand:        oklch(0.74 0.16 295);
  --brand-strong: oklch(0.80 0.15 295);
  --brand-soft:   oklch(0.74 0.16 295 / 0.16);
  --brand-border: oklch(0.74 0.16 295 / 0.40);
  --brand-text:   oklch(0.84 0.13 295);

  --spec-1: oklch(0.74 0.20 305);
  --spec-2: oklch(0.78 0.16 260);
  --spec-3: oklch(0.84 0.13 210);
  --spec-4: oklch(0.86 0.14 75);

  --grad-aurora: linear-gradient(115deg, var(--spec-1) 0%, var(--spec-2) 40%, var(--spec-3) 75%, var(--spec-4) 100%);
  --grad-brand:  linear-gradient(120deg, oklch(0.72 0.20 305) 0%, oklch(0.78 0.16 275) 100%);
  --grad-rim:    linear-gradient(90deg, transparent 0%, oklch(0.74 0.20 305 / 0.65) 25%, oklch(0.84 0.13 220 / 0.65) 60%, transparent 100%);

  --ok:          oklch(0.74 0.14 155);
  --ok-fill:     oklch(0.74 0.14 155 / 0.16);
  --ok-border:   oklch(0.74 0.14 155 / 0.36);
  --ok-text:     oklch(0.84 0.12 155);

  --warn:        oklch(0.80 0.14 65);
  --warn-fill:   oklch(0.80 0.14 65 / 0.16);
  --warn-border: oklch(0.80 0.14 65 / 0.36);
  --warn-text:   oklch(0.88 0.12 65);

  --danger:        oklch(0.74 0.17 25);
  --danger-fill:   oklch(0.74 0.17 25 / 0.16);
  --danger-border: oklch(0.74 0.17 25 / 0.36);
  --danger-text:   oklch(0.84 0.13 25);

  --shadow-card: none;
  --shadow-pop:  none;
}

/* Auto-follow OS when user hasn't explicitly chosen */
@media (prefers-color-scheme: dark) {
  html:not(.light):not([data-theme="light"]) {
    --bg-page:   oklch(0.18 0.012 250);
    --bg-panel:  oklch(0.20 0.012 250);
    --bg-card:   oklch(0.22 0.013 250);
    --bg-soft:   oklch(0.25 0.014 250);
    --bg-hover:  oklch(0.32 0.016 250 / 0.45);

    --border:        oklch(0.92 0.01 250 / 0.10);
    --border-soft:   oklch(0.92 0.01 250 / 0.05);
    --border-strong: oklch(0.92 0.01 250 / 0.22);

    --text:        oklch(0.97 0.005 250);
    --text-muted:  oklch(0.74 0.012 250);
    --text-soft:   oklch(0.62 0.012 250);
    --text-subtle: oklch(0.52 0.012 250);

    --brand:        oklch(0.74 0.16 295);
    --brand-strong: oklch(0.80 0.15 295);
    --brand-soft:   oklch(0.74 0.16 295 / 0.16);
    --brand-border: oklch(0.74 0.16 295 / 0.40);
    --brand-text:   oklch(0.84 0.13 295);

    --spec-1: oklch(0.74 0.20 305);
    --spec-2: oklch(0.78 0.16 260);
    --spec-3: oklch(0.84 0.13 210);
    --spec-4: oklch(0.86 0.14 75);

    --grad-aurora: linear-gradient(115deg, var(--spec-1) 0%, var(--spec-2) 40%, var(--spec-3) 75%, var(--spec-4) 100%);
    --grad-brand:  linear-gradient(120deg, oklch(0.72 0.20 305) 0%, oklch(0.78 0.16 275) 100%);
    --grad-rim:    linear-gradient(90deg, transparent 0%, oklch(0.74 0.20 305 / 0.65) 25%, oklch(0.84 0.13 220 / 0.65) 60%, transparent 100%);

    --ok:          oklch(0.74 0.14 155);
    --ok-fill:     oklch(0.74 0.14 155 / 0.16);
    --ok-border:   oklch(0.74 0.14 155 / 0.36);
    --ok-text:     oklch(0.84 0.12 155);

    --warn:        oklch(0.80 0.14 65);
    --warn-fill:   oklch(0.80 0.14 65 / 0.16);
    --warn-border: oklch(0.80 0.14 65 / 0.36);
    --warn-text:   oklch(0.88 0.12 65);

    --danger:        oklch(0.74 0.17 25);
    --danger-fill:   oklch(0.74 0.17 25 / 0.16);
    --danger-border: oklch(0.74 0.17 25 / 0.36);
    --danger-text:   oklch(0.84 0.13 25);

    --shadow-card: none;
    --shadow-pop:  none;
  }
}

/* Page-level dark effects */
html.dark .page-wash,
[data-theme="dark"] .page-wash {
  background:
    radial-gradient(900px 420px at 12% -10%, oklch(0.74 0.20 305 / 0.10), transparent 60%),
    radial-gradient(820px 380px at 88% -8%,  oklch(0.84 0.13 210 / 0.08),  transparent 60%);
}

html.dark .aurora-glow::after,
[data-theme="dark"] .aurora-glow::after {
  background: radial-gradient(60% 80% at 50% 50%, oklch(0.74 0.20 305 / 0.28), transparent 70%);
  filter: blur(28px);
}
```

- [ ] **Step 2: Verify no syntax errors**

Run: `cd /path/to/depsilo/web && npx tsc --noEmit`
Expected: 0 errors (CSS changes don't affect TS)

---

### Task 2: Replace ThemeToggle stub

**Files:**
- Modify: `web/src/components/ThemeToggle.tsx` (full rewrite)

- [ ] **Step 1: Replace file contents**

```tsx
import * as React from 'react'

type Theme = 'light' | 'dark' | 'system'
const STORAGE_KEY = 'theme'

function readTheme(): Theme {
  try {
    const v = localStorage.getItem(STORAGE_KEY)
    if (v === 'light' || v === 'dark' || v === 'system') return v
  } catch {}
  return 'system'
}

function writeTheme(t: Theme) {
  try { localStorage.setItem(STORAGE_KEY, t) } catch {}
}

function applyTheme(t: Theme) {
  const root = document.documentElement
  root.classList.remove('light', 'dark')
  if (t === 'light') root.classList.add('light')
  else if (t === 'dark') root.classList.add('dark')
}

export function useTheme(): [Theme, (t: Theme) => void] {
  const [theme, setTheme] = React.useState<Theme>(readTheme)

  React.useEffect(() => {
    applyTheme(theme)
    writeTheme(theme)

    if (theme === 'system') {
      const mq = window.matchMedia('(prefers-color-scheme: dark)')
      const onChange = () => applyTheme('system')
      mq.addEventListener('change', onChange)
      return () => mq.removeEventListener('change', onChange)
    }
  }, [theme])

  return [theme, setTheme]
}

const ICONS: Record<Theme, React.ReactNode> = {
  light: (
    <svg width="13" height="13" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <circle cx="8" cy="8" r="3" stroke="currentColor" strokeWidth="1.2" />
      <g stroke="currentColor" strokeWidth="1.2" strokeLinecap="round">
        <path d="M8 1.5v1.5M8 13v1.5M14.5 8H13M3 8H1.5M12.6 3.4l-1 1M4.4 11.6l-1 1M12.6 12.6l-1-1M4.4 4.4l-1-1" />
      </g>
    </svg>
  ),
  dark: (
    <svg width="13" height="13" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <path d="M13 9.5A5.5 5.5 0 016.5 3a5.5 5.5 0 106.5 6.5z" stroke="currentColor" strokeWidth="1.2" strokeLinejoin="round" />
    </svg>
  ),
  system: (
    <svg width="13" height="13" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <rect x="2" y="3" width="12" height="8.5" rx="1.5" stroke="currentColor" strokeWidth="1.2" />
      <path d="M5.5 14h5M8 11.5V14" stroke="currentColor" strokeWidth="1.2" strokeLinecap="round" />
    </svg>
  ),
}

const LABELS: Record<Theme, string> = {
  light: 'Light',
  dark: 'Dark',
  system: 'Auto',
}

export default function ThemeToggle() {
  const [theme, setTheme] = useTheme()
  const options: Theme[] = ['light', 'dark', 'system']

  return (
    <div
      role="radiogroup"
      aria-label="Theme"
      style={{
        display: 'inline-flex',
        padding: 2,
        border: '0.5px solid var(--border)',
        borderRadius: 7,
        background: 'var(--bg-soft)',
        gap: 1,
      }}
    >
      {options.map((opt) => {
        const on = theme === opt
        return (
          <button
            key={opt}
            type="button"
            role="radio"
            aria-checked={on}
            aria-label={LABELS[opt]}
            title={LABELS[opt]}
            onClick={() => setTheme(opt)}
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 5,
              height: 24,
              padding: '0 8px',
              borderRadius: 5,
              border: 0,
              background: on ? 'var(--bg-card)' : 'transparent',
              color: on ? 'var(--text)' : 'var(--text-muted)',
              fontSize: 11,
              fontWeight: on ? 600 : 500,
              letterSpacing: '0.005em',
              boxShadow: on ? '0 0 0 0.5px var(--border), 0 1px 0 rgba(0,0,0,0.04)' : 'none',
              cursor: 'pointer',
              transition: 'background 120ms ease, color 120ms ease',
            }}
          >
            {ICONS[opt]}
            <span>{LABELS[opt]}</span>
          </button>
        )
      })}
    </div>
  )
}
```

---

### Task 3: Anti-FOUC script in index.html

**Files:**
- Modify: `web/index.html`

- [ ] **Step 1: Add inline script before closing `</head>`**

```html
<script>
  (function(){
    var t = localStorage.getItem('theme');
    if (t === 'dark') document.documentElement.classList.add('dark');
    else if (t === 'light') document.documentElement.classList.add('light');
  })();
</script>
```

---

### Task 4: Add page-wash div to PortalApp

**Files:**
- Modify: `web/src/portal/PortalApp.tsx` (line 79, inside root div)

- [ ] **Step 1: Add `<div className="page-wash" />` as first child**

Inside the root `<div className="min-h-screen" ...>`, add as first child:
```tsx
<div className="page-wash" />
```

---

### Task 5: Build verification

- [ ] **Step 1: Run TypeScript check**

Run: `cd web && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 2: Run frontend build**

Run: `make frontend`
Expected: Build succeeds, no errors
