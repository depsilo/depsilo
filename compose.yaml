services:
  depsilo:
    image: ${DEPSILO_IMAGE:-ghcr.io/depsilo/depsilo:latest}
    container_name: depsilo
    ports:
      - "${DEPSILO_BIND_ADDRESS:-0.0.0.0}:${PORT:-23333}:23333"
    volumes:
      - depsilo-data:/root/.depsilo
    restart: unless-stopped

volumes:
  depsilo-data:
