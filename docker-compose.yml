version: '3.8'
services:
  repocache:
    build: .
    ports:
      - "23333:23333"
    volumes:
      - ./data:/app/data
      - ./config.toml:/app/config.toml
    environment:
      - REPOCACHE_CONFIG=/app/config.toml
    restart: unless-stopped
