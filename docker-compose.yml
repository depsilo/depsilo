version: '3.8'
services:
  depslio:
    build: .
    ports:
      - "23333:23333"
    volumes:
      - ./data:/app/data
      - ./config.toml:/app/config.toml
    environment:
      - DEPSLIO_CONFIG=/app/config.toml
    restart: unless-stopped
