# Impeccable upstream

This project-local Codex skill is vendored from
[`pbakaus/impeccable`](https://github.com/pbakaus/impeccable) at commit
`1cf7d7ab0f1ac0bb3319fd20be389a3009f4037d` (npm package version `3.4.0`,
skill schema version `4.0.3`).

The vendored files are unmodified. Update them deliberately from a reviewed,
fixed upstream revision; do not float to `main` in CI.
